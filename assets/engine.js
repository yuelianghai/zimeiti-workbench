/* ===== 爆款文案引擎 =====
   结构：痛点钩子 → 引导收藏 → 干货正文(步骤式/清单式/分点式) → 金句 → 行动号召
   规则：标题说几条 = 正文几条；每条 = 具体做法 + 小提醒；口语化；融入当日爆款元素
*/
(function (w) {
  'use strict';

  var R = function (a) { return a[Math.floor(Math.random() * a.length)]; };
  function pickN(arr, n) {
    var c = arr.slice(), out = [];
    while (out.length < n && c.length) out.push(c.splice(Math.floor(Math.random() * c.length), 1)[0]);
    while (out.length < n) out.push(arr[out.length % arr.length]);
    return out;
  }
  function headN(arr, n) {
    var out = arr.slice(0, n);
    while (out.length < n) out.push(arr[out.length % arr.length]);
    return out;
  }

  /* ---------------- 赛道要点库：每条 = 具体做法 + 小提醒 ---------------- */
  var LIB = {
    ai: {
      name: 'AI工具 / AI变现',
      kw: ['ai', 'chatgpt', 'claude', 'gpt', '提示词', '智能体', '大模型', '自动化', '数字人', 'midjourney', '即梦', '剪映', 'sora', '工具', '效率', 'agent', 'coze', '扣子'],
      step: [
        { t: '先把需求写成一句人话', d: '别一上来就找工具。拿张纸写清楚「我要谁、在什么场景、拿到什么结果」，一句话说不明白的需求，AI 也做不明白。', tip: '写完念一遍，有形容词就删掉，换成能验收的动词。' },
        { t: '选工具只看一个标准：免费额度够不够你跑完一次完整流程', d: '别被功能列表忽悠。开一个小号，用免费额度把你的真实任务从头跑到尾，跑不完的直接淘汰。', tip: '注册前先搜「工具名 + 免费额度」，能省下一半试错时间。' },
        { t: '把提示词拆成四块：身份 + 任务 + 限制 + 示例', d: '身份决定语气，任务决定方向，限制决定不跑偏，示例决定像不像。前三块很多人会写，第四块才是拉开差距的地方。', tip: '示例给 1 个就够，给多了 AI 会照抄不会举一反三。' },
        { t: '第一版永远是废稿，重点是怎么改第二版', d: '别问「帮我重写」，要指着具体位置说「第二段太官方，改成跟朋友吐槽的语气，保留数据」。改一次比重生成十次有用。', tip: '把你改过的地方存成一份「我的修改清单」，下次直接贴进提示词里。' },
        { t: '给输出定一个能验收的格式', d: '要表格就说清楚几列分别是什么，要文案就说清楚几段每段多少字。格式一定，返工率立刻降下来。', tip: '格式写在提示词最后一行，模型对结尾的指令最敏感。' },
        { t: '把跑通的流程固化成模板', d: '同一类活儿别每次重来。建个备忘录，把「提示词原文 + 参数 + 满意的输出」三件套存一起，下次改两个词就能复用。', tip: '模板命名带上日期，工具更新后老模板可能失效。' },
        { t: '人工最后过一遍，专门删 AI 味', d: '重点删三样：排比句、「不仅...而且」、没有主语的宏大总结。再补三样：具体数字、真实场景、你自己的口头禅。', tip: '念出声。念着别扭的句子，读者刷到也别扭。' },
        { t: '把成品变成可交付的东西', d: '导成 PDF、做成模板包、录成一条教程。能交付才能收钱，停在对话框里的都是自嗨。', tip: '第一份交付物定价压低，先换 3 个真实评价。' }
      ],
      list: [
        { t: '免费额度够用的主力工具', d: '一个对话型的负责写和想，一个绘图型的负责封面，一个剪辑型的负责成片。三件套凑齐就能开工。', tip: '别同时开五个会员，先用免费版把流程跑顺。' },
        { t: '提示词库', d: '按用途分文件夹存：标题、正文、脚本、改写、总结。每条注明「适合什么场景、别用在哪」。', tip: '存的时候连输出一起存，光存提示词以后看不懂。' },
        { t: '素材备份的习惯', d: '每次生成的满意结果立刻存一份到本地，云端服务改版或者限额，历史记录说没就没。', tip: '按「日期_主题」命名，别用默认文件名。' },
        { t: '一份自己的风格说明', d: '写清楚你常用的口头禅、句子长度、爱不爱用emoji、忌讳哪些词。以后每次贴进去，输出立刻像你。', tip: '这份说明每两周更新一次，你的风格是会变的。' },
        { t: '效果对照表', d: '同一个需求让两个工具各出一版，把结果贴在一起对比。用数据决定主力工具，别凭感觉。', tip: '对比时控制变量，提示词必须一模一样。' },
        { t: '一条随时能接单的报价链路', d: '闲鱼/小红书挂一个具体服务，写清交付时间和修改次数。有人问价就是验证，没人问就换个说法。', tip: '别写「可议价」，标个具体数字反而咨询更多。' },
        { t: '定期清理的机制', d: '每月底删掉一个月没用过的工具账号和订阅，AI 工具更新太快，留着只会分散注意力。', tip: '删之前把里面的历史记录导出来。' },
        { t: '一个失败案例记录本', d: '记下每次翻车：什么需求、什么工具、错在哪。这个本子比任何教程都值钱。', tip: '每条不超过三行，太长你不会回头看。' }
      ],
      view: [
        { t: 'AI 不会淘汰你，会淘汰「只会重复劳动」的那部分你', d: '真正被替代的是流程化的部分。把重复的交出去，把判断和审美留下来，这才是分工。', tip: '每周挑一件最烦的重复活儿，交给 AI 试一次。' },
        { t: '工具越多，产出越少', d: '收藏了 50 个工具但一个都没跑通，等于零。挑三个用到吐，比试五十个有用得多。', tip: '给自己定个规矩：新工具进来一个，旧的淘汰一个。' },
        { t: '提示词的天花板是你的表达能力', d: 'AI 只能理解你说清楚的东西。你脑子里模糊的部分，它一定会给你一个模糊的答案。', tip: '写不清楚的时候，先讲给一个外行朋友听。' },
        { t: '别追新模型，追你的交付质量', d: '换模型带来的提升，通常不如你多改两遍。用户看到的是结果，不是你用了哪个版本。', tip: '每次想换工具前，先问「现在这个我用到极限了吗」。' },
        { t: 'AI 生成的内容不等于你的作品', d: '你的判断、取舍、经验，才是署名的理由。原封不动发出去的，读者一眼能看出来。', tip: '每篇至少改动 30% 的表达，加进你的真实经历。' },
        { t: '变现的顺序是「先解决具体的人的具体问题」', d: '不是先做账号再想变现。先找到一个愿意付钱的人，把他的问题解决好，路径自然出来。', tip: '第一个客户从熟人圈找，反馈最真实。' }
      ]
    },

    money: {
      name: '副业 / 搞钱',
      kw: ['副业', '搞钱', '赚钱', '变现', '接单', '兼职', '收入', '存钱', '理财', '第二职业', '睡后收入'],
      step: [
        { t: '先算清楚你每天真能拿出多少时间', d: '不是「有空就做」，是具体到「晚上 9 点到 10 点半」。时间不确定的副业，做不过三周。', tip: '第一周先只投入一半时间，留缓冲。' },
        { t: '按「时间换钱」还是「一次做多次卖」分类', d: '前者稳但有上限，后者慢但能复利。新手先做前者拿到正反馈，再拿收入养后者。', tip: '两条腿走路，别一上来就all in后者。' },
        { t: '找到第一个愿意付钱的人', d: '别做完整产品再找客户。先在闲鱼/小红书挂一个具体服务，有人问价才说明需求真实。', tip: '第一单可以便宜，但一定要收钱，白送换不来真实反馈。' },
        { t: '把交付标准写死在成交前', d: '做几版、多久交、改几次、超出怎么算钱，一条条列在聊天记录里。', tip: '截图存档，比口头承诺管用一百倍。' },
        { t: '前 5 单专攻好评', d: '定价压到市场价七折，但服务做到十二分。评价是新手唯一的信任凭证。', tip: '交付后主动问「有没有可以改进的」，顺势请对方留评。' },
        { t: '收入达到稳定线后立刻涨价', d: '连续两周订单接不完，就是涨价信号。涨 20%，流失的是最难伺候的那批。', tip: '涨价前给老客户发一次通知，保留一次原价机会。' },
        { t: '把流程记成 SOP，为下一步做准备', d: '每一步写清楚做什么、用什么工具、多久完成。这份文档以后可以外包，也可以变成课。', tip: '边做边记，事后补一定会漏。' }
      ],
      list: [
        { t: '零门槛型：大厂众包 / 数据标注', d: '百度众测、腾讯搜活帮、阿里众包，官方入口零押金，时薪 15-40 元，碎片时间就能做。', tip: '凡是要交押金、买工号的一律是骗子。' },
        { t: '轻技能型：短视频代剪', d: '给本地商家剪片，30-200 元一条。剪映免费版足够，先剪三条样片当作品集。', tip: '先收 50% 定金，素材要求对方一次发齐。' },
        { t: '信息差型：虚拟资源整理', d: '把散落的模板、清单、资料整理成一份能直接用的文件，9.9-19.8 元卖。零边际成本。', tip: '只卖你有权分发的内容，盗版会封号。' },
        { t: '顺手型：地图拍照任务', d: '高德淘金、美团拍店，通勤路上拍门头照，1-5 元一条，一次规划 5-10 个点。', tip: '当零花补充，别当主业指望。' },
        { t: '稳定型：居家云客服', d: '美团、拼多多、顺丰官方招募，时薪 22-38 元，晚高峰有补贴。', tip: '打字速度是硬指标，先练到 60 字/分钟。' },
        { t: '社区型：宠物代遛 / 上门喂猫', d: '业主群接单，遛狗 30-60 元一次，喂猫 50-80 元一次，节假日爆单。', tip: '每次上门拍视频反馈，这是复购的关键。' },
        { t: '长期型：内容分成', d: '微信搜一搜问答、头条、百家号，无粉丝门槛，回答真实通顺就有推荐。', tip: '每天 3-5 条，坚持 30 天再看曲线。' },
        { t: '避坑清单', d: '交押金的、要身份证正反面的、承诺日入过千的、先培训后上岗还收费的，全部拉黑。', tip: '正规平台永远不会让你先掏钱。' }
      ],
      view: [
        { t: '副业不是第二份班，是第二条路', d: '如果只是换个地方出卖时间，累垮了也涨不了收入。判断标准：这件事做三个月，会不会积累出可复用的东西。', tip: '每个月问自己一次：我攒下了作品、客户还是技能？' },
        { t: '起步阶段，速度比选择重要', d: '纠结一个月选哪个方向，不如做一周试错。真实的市场反馈，比任何分析都准。', tip: '给自己定死线：三天内必须挂出第一个服务。' },
        { t: '收入不稳的根源通常不是能力，是没有回头客', d: '一直找新客户最累。老客户复购一次，抵得上十次冷启动。', tip: '交付完隔一周主动回访一次，成本极低。' },
        { t: '别把「学习」当成拖延的借口', d: '课听了八节还没开单，问题不在知识不够，在没开始。边做边补，才是真的学。', tip: '规定自己：学一小时必须实践两小时。' },
        { t: '真实的门槛不是技能，是脸皮', d: '大部分人卡在「不好意思报价」和「怕做不好」。这两关过了，剩下的都是熟练度问题。', tip: '第一次报价前，先在心里把最坏结果想一遍。' }
      ]
    },

    content: {
      name: '内容创作 / 涨粉',
      kw: ['涨粉', '爆款', '文案', '选题', '账号', '运营', '短视频', '小红书', '抖音', '公众号', '标题', '流量', '完播', '起号', '剪辑', '脚本'],
      step: [
        { t: '选题从「搜索框」里找，别从脑子里找', d: '在平台搜你的关键词，把下拉联想词全抄下来。这些是真实有人在搜的需求，不是你以为的需求。', tip: '联想词后面带「怎么」「哪个好」的优先做，搜索意图最强。' },
        { t: '标题先写，内容后写', d: '标题定不下来说明选题没想清楚。先写 5 个标题，挑最想点的那个，再倒推内容。', tip: '标题里必须有一个具体的人（谁）和一个具体的结果（得到什么）。' },
        { t: '前三秒只干一件事：让人产生「这说的是我」', d: '不要问好、不要自我介绍、不要铺垫背景。直接甩出对方正在经历的那个具体场景。', tip: '把开头念给朋友听，他没抬头就是失败。' },
        { t: '中间每一段都要给一个能立刻用的动作', d: '不是「要重视封面」，是「封面字号调到能在缩略图看清，一般 6 个字以内」。', tip: '每写完一段，问一句「读者现在能做什么」。' },
        { t: '结尾留一个低成本互动', d: '不要问「你怎么看」这种开放题。问「你是 A 还是 B」，回复成本低，评论区才起得来。', tip: '自己在评论区先放一条答案，带节奏。' },
        { t: '发布后两小时是黄金观察期', d: '盯完播率和互动率，数据不对立刻改封面和标题，别等它凉透。', tip: '同一条内容，改标题重发不算重复内容，很多人不知道。' },
        { t: '每周做一次复盘', d: '把这周的数据拉出来，只看一个问题：跑得好的三条有什么共同点。', tip: '共同点写在便签上贴屏幕边，下周照着做。' }
      ],
      list: [
        { t: '保存型内容优先做', d: '清单、避坑、参数、模板这类内容，收藏率高、长尾搜索强，能持续给你带流量。', tip: '标题里加「收藏版」「照着抄」，收藏率能明显上一档。' },
        { t: '标题公式常备三套', d: '痛点型「XX的人别再XX了」、数字型「N个XX，第X个最有用」、反差型「我以为XX，结果XX」。', tip: '三套轮着用，别一个月都是同一个句式。' },
        { t: '封面三要素', d: '一个大字标题、一个人或物做视觉锚点、一个高对比配色。缩略图能看清才算合格。', tip: '做完把图缩到指甲盖大小看一眼。' },
        { t: '固定的内容栏目', d: '一周三条里，至少一条是固定栏目（比如「周三拆一个爆款」）。观众会形成期待。', tip: '栏目名要短，三到五个字。' },
        { t: '素材随手存的习惯', d: '刷到好的钩子、结构、金句，立刻截图存进素材库，标注「好在哪」。', tip: '只存不标注，等于没存。' },
        { t: '一条内容的多平台改法', d: '同一个选题，小红书写图文清单，抖音写口播脚本，视频号改成更慢的节奏。', tip: '别原封不动搬运，各平台的语感差别很大。' },
        { t: '数据看板', d: '每条记录：发布时间、标题、完播率、点赞收藏比。三十条之后规律自然浮现。', tip: '点赞收藏比比绝对数量更能说明内容质量。' }
      ],
      view: [
        { t: '爆款不是写出来的，是选出来的', d: '选题占七成，表达占三成。选错方向，文笔再好也救不回来。', tip: '每次动笔前，先确认这个话题有人在搜。' },
        { t: '你的粉丝不需要你完美，需要你真实', d: '翻车、犹豫、笨拙的过程，比精修的成品更容易带来关注。', tip: '每十条里放一条「不完美」的记录。' },
        { t: '更新频率的意义是练手，不是喂算法', d: '日更的价值在于逼你形成手感，而不是感动自己。手感有了，降频也能出爆款。', tip: '定一个能长期扛住的频率，别开局就日更。' },
        { t: '模仿不是抄，是拆结构', d: '拆钩子怎么切、干货怎么排、结尾怎么收，然后换成你自己的内容装进去。', tip: '拆完至少隔一天再写，避免用词雷同。' },
        { t: '数据差的时候，先改钩子不要改赛道', d: '大部分内容不是方向错，是前三秒没抓住。换赛道的成本远高于换开头。', tip: '同一篇内容换三个开头测一测。' }
      ]
    },

    life: {
      name: '生活 / 成长 / 通用',
      kw: ['习惯', '自律', '早起', '读书', '学习', '效率', '整理', '情绪', '焦虑', '成长', '规划', '时间管理', '健身', '减肥', '穿搭', '护肤', '装修', '收纳'],
      step: [
        { t: '把目标缩小到「明天就能做完」的尺寸', d: '不是「养成读书习惯」，是「明晚睡前读 5 页」。小到不可能失败，才启动得起来。', tip: '第一周故意定得偏低，先建立「我能做到」的感觉。' },
        { t: '给动作找一个固定的触发点', d: '挂在已有习惯后面：刷完牙就读书、通勤上车就听课。靠触发点，不靠意志力。', tip: '触发点必须是每天固定发生的事。' },
        { t: '把过程可视化', d: '打卡表、进度条、日历标记，看得见的累积会自己推着你往前。', tip: '断一天不要清零，允许自己「补卡」。' },
        { t: '设计一个失败预案', d: '提前想好「加班到十点怎么办」，答案是「那就只做三分钟版本」。有退路才不会全盘放弃。', tip: '三分钟版本要提前写下来，临场想不出来。' },
        { t: '每周固定时间复盘一次', d: '只问三个问题：哪天没做到、为什么、下周怎么绕开。', tip: '复盘控制在十分钟内，太长会变成新的负担。' },
        { t: '把成果拿出来见人', d: '发朋友圈、发社群、告诉一个朋友。外部反馈是最便宜的续航燃料。', tip: '找一个会认真回应你的人，而不是发给所有人。' }
      ],
      list: [
        { t: '一个固定的启动仪式', d: '一杯水、一首歌、一句口令，都行。重复几次之后，身体会自动进入状态。', tip: '仪式不超过两分钟，太长就成了拖延。' },
        { t: '把手机放到够不着的地方', d: '不是靠自制力关通知，是物理上拿不到。距离产生专注。', tip: '放门口鞋柜比放抽屉有效。' },
        { t: '每天只定三件必须完成的事', d: '写在便签上，做完划掉。超过三件，等于没有重点。', tip: '第一件必须是最难的那件。' },
        { t: '固定的休息节奏', d: '专注 45 分钟休息 8 分钟，休息时站起来走动，别刷手机。', tip: '定闹钟，别靠感觉判断时间。' },
        { t: '一份「不做清单」', d: '写清楚哪些事你今年不碰。拒绝的能力，比努力更能决定进度。', tip: '每季度重新审一次，有些拒绝会过期。' },
        { t: '睡前十分钟的整理', d: '收桌面、列明天的三件事。第二天的启动速度取决于前一晚。', tip: '写明天的事，不要写今天的总结。' }
      ],
      view: [
        { t: '坚持不下去，通常是目标定得太大', d: '不是你懒，是你给自己安排了一个只有超人才能完成的计划。', tip: '把当前目标砍一半，先跑通一个月。' },
        { t: '状态好的时候做计划，状态差的时候执行计划', d: '别在低谷期做重大决定，也别指望永远高能。系统比状态可靠。', tip: '把计划写下来，低谷期照着做就行。' },
        { t: '进步是台阶式的，不是斜坡', d: '很长一段时间看不到变化，然后突然上一个台阶。平台期是常态，不是失败。', tip: '平台期就记录过程，别看结果。' },
        { t: '你不需要更多方法，需要重复一个方法', d: '收藏了三十套方法论却一套没跑完，问题不在方法。', tip: '挑一个用满三十天再评价。' },
        { t: '允许自己做得难看', d: '第一次都是笨拙的。追求一次到位，就永远等不到开始。', tip: '给自己一个「烂开头」的额度。' }
      ]
    }
  };

  function matchLib(topic) {
    var t = (topic || '').toLowerCase(), best = null, score = 0;
    Object.keys(LIB).forEach(function (k) {
      var s = 0;
      LIB[k].kw.forEach(function (w2) { if (t.indexOf(w2) >= 0) s += w2.length; });
      if (s > score) { score = s; best = k; }
    });
    return best || 'content';
  }

  /* ---------------- 钩子 / 收藏引导 / 金句 / CTA ---------------- */
  var HOOK = {
    痛点: [
      '真的别再{wrong}了。我{time}就是这么干的，结果{bad}。',
      '{aud}最怕的不是不会{topic}，是花了大把时间还在原地打转。',
      '如果你也卡在{topic}这一步，先别急着找教程，问题大概率不在这儿。',
      '每次说到{topic}，我都想拉住那个{time}的自己：别再瞎折腾了。',
      '为什么你研究了那么久{topic}，还是做不出结果？答案挺扎心的。'
    ],
    反差: [
      '我以为{topic}要花大力气，结果{n}步就搞定了，还比之前效果好。',
      '别人做{topic}三个月才起步，我用{n}个方法，第{k}天就有反馈。',
      '一开始我也觉得{topic}是玄学，直到我把它拆成{n}块。',
      '看起来最麻烦的{topic}，其实最省力的做法只有{n}条。',
      '我劝退过自己 3 次的{topic}，最后靠这{n}点做成了。'
    ],
    数字: [
      '花了{time}踩坑，我把{topic}总结成{n}条，直接照着做就行。',
      '{n}条关于{topic}的实操，每条都写清了怎么做和会踩什么坑。',
      '试了十几种方法之后，真正有用的{topic}方法就这{n}个。',
      '这{n}条{topic}的经验，是我用真金白银换来的，建议先收藏。'
    ],
    情绪: [
      '昨晚又因为{topic}折腾到半夜，today 我决定把这套流程写清楚。',
      '说个有点丢人的事：我在{topic}上栽过的跟头，比做成的次数还多。',
      '有些话憋很久了，关于{topic}，今天一次说完。',
      '如果你现在也在为{topic}焦虑，这条可能刚好能接住你。'
    ],
    悬念: [
      '{topic}这件事，最关键的一步 90% 的人都跳过了。',
      '同样做{topic}，为什么有人一周出结果，有人三个月还在准备？差别就在这{n}点。',
      '关于{topic}，有个反常识的结论，可能会颠覆你现在的做法。'
    ],
    身份: [
      '作为一个把{topic}反复做砸又爬起来的人，我总结了{n}条。',
      '如果你是{aud}，这{n}条{topic}经验能帮你少走大半年弯路。',
      '写给正在起步的{aud}：{topic}真的没那么复杂，就{n}件事。'
    ]
  };

  var SAVE_TIP = [
    '内容有点长，建议先点收藏再看，不然刷走就找不到了。',
    '这套东西我整理了很久，先收藏，用的时候直接翻出来照做。',
    '往下都是干货，怕忘的话先收藏 + 关注，后面还会更新。',
    '强烈建议收藏，这几条我自己现在还在天天用。',
    '看完记得收藏，明天做的时候一条条对着checklist走。'
  ];

  var GOLD = [
    '{topic}这件事，难的从来不是方法，是你愿不愿意先做出一个难看的版本。',
    '别人的经验只能帮你避坑，真正的路还得你自己走一遍才认得。',
    '所有看起来轻松的结果，背后都是把复杂的事拆成了简单的动作。',
    '慢一点没关系，方向对了，慢就是快。',
    '把标准降到能开始，把要求提到能交付，中间那段就是你的成长。',
    '你不需要一次做对，只需要每次比上次少错一点。',
    '与其等准备好，不如在做的过程中准备。',
    '真正拉开差距的，从来不是知道多少，而是做完了多少。'
  ];

  var CTA = {
    小红书: [
      '如果这条对你有用，点个收藏，明天开始就照着做一条。\n评论区告诉我你卡在第几步，我一条条回。',
      '先收藏，再从第 1 条开始做。做完回来评论区打个卡，我看到都会回。\n你现在是「完全没开始」还是「做了但没效果」？扣 1 或 2。',
      '别只收藏不动手。今晚就挑一条最容易的做掉，明天来评论区跟我说结果。'
    ],
    抖音: [
      '觉得有用点个赞，评论区扣「1」我出下一条更细的拆解。\n关注我，每天一条能立刻上手的干货。',
      '这条先收藏，做的时候拿出来对着做。\n你卡在哪一步？评论区打出来，下条视频专门讲。',
      '点赞过千我就把完整模板放评论区。关注一下，别刷丢了。'
    ],
    视频号: [
      '觉得有帮助的话，点个「赞」和「在看」，也可以转发给正需要的朋友。\n评论区聊聊你的情况，我看到会回。',
      '内容整理不易，点个在看，转给身边同样在琢磨这件事的人。'
    ],
    B站: [
      '三连支持一下，下期我把每一步都录成实操演示。\n评论区留言你想看哪一部分展开讲。',
      '如果这期有用，点个收藏和关注。有问题评论区问，我都会看。'
    ],
    公众号: [
      '这套方法我会持续更新，点个「在看」，也欢迎转发给需要的朋友。\n留言区说说你的情况，我挑典型的展开写一篇。'
    ]
  };

  var AUD = ['新手', '刚起步的人', '上班族', '普通人', '想做副业的人', '自媒体新人'];
  var TIME = ['刚开始那阵', '去年', '前三个月', '刚入行的时候'];
  var BAD = ['白白浪费了两个月', '一条也没跑起来', '钱花了效果为零', '越做越没信心'];

  function fill(tpl, v) {
    return tpl.replace(/\{(\w+)\}/g, function (m, k) { return v[k] !== undefined ? v[k] : m; });
  }

  /* ---------------- 标题 ---------------- */
  var TITLE = {
    小红书: [
      '{topic}的{n}个方法，第{k}个我用到现在📌',
      '{aud}必看｜{topic}就这{n}步，照抄就行✨',
      '花了{time}才明白｜{topic}的{n}条真话💡',
      '别再瞎折腾{topic}了！这{n}点才是关键🔥',
      '{topic}保姆级{n}步，新手今晚就能开始🌸',
      '我把{topic}拆成了{n}条，收藏版建议存下📎',
      '{n}条{topic}避坑指南，早看早省事⚠️',
      '关于{topic}，我想说{n}句大实话🫶'
    ],
    抖音: [
      '{topic}只需要{n}步，第{k}步最容易被忽略',
      '{n}个{topic}方法，看完直接抄',
      '别人不会告诉你的{n}个{topic}真相',
      '{topic}做不好？大概率错在这{n}点',
      '{time}踩坑总结的{n}条{topic}经验'
    ],
    视频号: [
      '{topic}的{n}个要点，说得很实在',
      '关于{topic}，这{n}条建议值得看完',
      '{n}分钟讲清{topic}，收藏起来慢慢看'
    ],
    B站: [
      '【干货】{topic}完整拆解｜{n}个关键步骤',
      '我把{topic}研究透了，总结成这{n}点',
      '{topic}从入门到能交付｜{n}步实操'
    ],
    公众号: [
      '{topic}的{n}个方法：从入门到跑通',
      '我用{time}总结的{n}条{topic}经验，值得收藏',
      '关于{topic}，这{n}件事没人明说'
    ]
  };

  /* ---------------- 主生成函数 ---------------- */
  function generate(opt) {
    var topic = (opt.topic || '这件事').trim();
    var platform = opt.platform || '小红书';
    var type = opt.type || '步骤式';
    var n = Math.max(1, Math.min(12, parseInt(opt.n, 10) || 5));
    var hookStyle = opt.hook || '痛点';
    var aud = opt.audience || R(AUD);
    var trend = opt.trend || null;      // 当日同赛道爆款上下文
    var userPoints = (opt.points || '').split('\n').map(function (s) { return s.trim(); }).filter(Boolean);

    var libKey = matchLib(topic);
    var lib = LIB[libKey];
    var poolKey = type === '步骤式' ? 'step' : (type === '清单式' ? 'list' : 'view');
    var pool = lib[poolKey] || lib.step;

    // 干货条目：优先用用户自己的要点
    var items = [];
    if (userPoints.length) {
      for (var i = 0; i < n; i++) {
        var up = userPoints[i];
        if (up) {
          var parts = up.split(/[｜|]/);
          items.push({
            t: parts[0].trim(),
            d: (parts[1] || '').trim() || '把这一条落到具体动作上：今天先做最小的那一步，做完再想下一步。',
            tip: (parts[2] || '').trim() || '做的时候记一句备注，下次就不用重新想。'
          });
        } else {
          items.push(pool[(i - userPoints.length + pool.length) % pool.length]);
        }
      }
    } else {
      items = type === '步骤式' ? headN(pool, n) : pickN(pool, n);
    }

    var k = Math.min(items.length, 2 + Math.floor(Math.random() * Math.max(1, n - 2)));
    var vars = {
      topic: topic, n: n, k: k, aud: aud,
      time: R(TIME), bad: R(BAD), wrong: '硬啃' + topic
    };

    // 标题（数量与正文对齐）
    var tps = TITLE[platform] || TITLE['小红书'];
    var titles = pickN(tps, Math.min(4, tps.length)).map(function (t) { return fill(t, vars); });

    // 钩子
    var hookPool = HOOK[hookStyle] || HOOK['痛点'];
    var hook = fill(R(hookPool), vars);

    // 融入当日爆款味道
    var trendLine = '';
    if (trend && trend.title) {
      var flavors = [
        '（顺带一提，今天「' + trend.title + '」正在平台上跑，做' + topic + '的话可以蹭一下这个情绪）',
        '最近刷到「' + trend.title + '」很火，其实底层逻辑和' + topic + '是一回事：都在解决同一种焦虑。',
        '今天平台上「' + trend.title + '」热度不低，你完全可以把它当外壳，把' + topic + '当内核。'
      ];
      trendLine = R(flavors);
    }

    // 正文
    var body = [];
    body.push(hook);
    if (trendLine) body.push(trendLine);
    body.push('');
    body.push(R(SAVE_TIP));
    body.push('');

    var head = { '步骤式': '第 %d 步', '清单式': '%d.', '分点式': '%d.' }[type] || '%d.';
    var em = ['1️⃣', '2️⃣', '3️⃣', '4️⃣', '5️⃣', '6️⃣', '7️⃣', '8️⃣', '9️⃣', '🔟', '1️⃣1️⃣', '1️⃣2️⃣'];

    items.slice(0, n).forEach(function (it, idx) {
      var no = idx + 1;
      var label = type === '步骤式'
        ? em[idx] + ' 第' + no + '步：' + it.t
        : em[idx] + ' ' + it.t;
      body.push(label);
      body.push(it.d);
      body.push('👉 小提醒：' + it.tip);
      body.push('');
    });

    // 金句
    body.push('—');
    body.push('💗 ' + fill(R(GOLD), vars));
    body.push('');

    // CTA
    body.push((CTA[platform] || CTA['小红书'])[Math.floor(Math.random() * (CTA[platform] || CTA['小红书']).length)]);

    // 标签
    var baseTags = ['#' + topic.replace(/\s/g, ''), '#' + lib.name.split(' / ')[0], '#干货分享', '#' + aud];
    if (trend && trend.keywords) trend.keywords.slice(0, 2).forEach(function (x) { baseTags.push('#' + x); });
    var tags = baseTags.filter(function (v, i, a) { return a.indexOf(v) === i; }).slice(0, 8).join(' ');

    var text = body.join('\n').replace(/\n{3,}/g, '\n\n');

    // 抖音额外给分镜
    var shots = null;
    if (platform === '抖音' || platform === '视频号' || platform === 'B站') {
      shots = buildShots(topic, items.slice(0, n), hook);
    }

    return {
      titles: titles,
      title: titles[0],
      body: text,
      tags: tags,
      shots: shots,
      meta: { lib: lib.name, type: type, n: n, platform: platform, hook: hookStyle, words: text.length }
    };
  }

  function buildShots(topic, items, hook) {
    var s = [];
    s.push({ t: '0-3s', v: '正面出镜/大字卡', a: hook.slice(0, 40), n: '第一帧必须有大字，静音也能看懂' });
    s.push({ t: '3-8s', v: '切近景 + 手势', a: '这条讲完你就能直接上手，' + items.length + '条，一条不废话。', n: '给出承诺，降低划走率' });
    items.forEach(function (it, i) {
      s.push({
        t: (8 + i * 12) + '-' + (20 + i * 12) + 's',
        v: '画面切换：' + (i % 2 ? '屏幕录制 / 实拍演示' : '出镜口播 + 字幕条'),
        a: '第' + (i + 1) + '，' + it.t + '。' + it.d,
        n: '关键词打在屏幕上：' + it.t
      });
    });
    var last = 8 + items.length * 12;
    s.push({ t: last + '-' + (last + 8) + 's', v: '停顿 + 金句大字', a: '记住一句话就够了。', n: '这里放金句，节奏放慢' });
    s.push({ t: (last + 8) + 's-结尾', v: '看镜头 + 引导手势', a: '卡在哪一步？评论区打出来。', n: '互动结尾，别说「谢谢观看」' });
    return s;
  }

  /* ---------------- 爆款拆解 ---------------- */
  var EMO = ['崩溃', '焦虑', '爽', '后悔', '心疼', '离谱', '救命', '绝了', '值了', '扎心', '窒息', '治愈', '破防', '真的服了', '狠狠', '啊啊啊'];
  var HOOKSIG = [
    { k: /^(别再|千万别|不要再|求你别)/, n: '劝阻式钩子', d: '用否定命令制造紧张，读者会想确认自己是不是正在犯错' },
    { k: /(我以为|结果|没想到|居然|竟然)/, n: '反差钩子', d: '预期与结果的落差是最强的注意力抓手' },
    { k: /\d+\s*(个|条|步|天|块|元|万|%)/, n: '数字钩子', d: '具体数字提供确定性，降低点开的心理成本' },
    { k: /(为什么|凭什么|怎么才能|到底)/, n: '疑问钩子', d: '开放式问题激活好奇，读者需要一个答案才能闭环' },
    { k: /(如果你|你是不是|有没有人|谁懂)/, n: '身份代入钩子', d: '直接点名目标人群，无关的人自动划走，精准度反而更高' },
    { k: /(我|自己|亲测|实测|踩坑)/, n: '第一人称经验钩子', d: '真实经历自带信任，比结论更容易被接受' }
  ];

  function analyze(txt) {
    var t = (txt || '').trim();
    if (!t) return null;
    var lines = t.split('\n').filter(function (x) { return x.trim(); });
    var first = lines[0] || '';
    var words = t.length;

    var hooks = HOOKSIG.filter(function (h) { return h.k.test(first) || h.k.test(t.slice(0, 80)); });
    if (!hooks.length) hooks = [{ n: '平铺直叙', d: '开头没有明显钩子，这是最容易掉完播的地方' }];

    var nums = (t.match(/\d+(\.\d+)?\s*(个|条|步|天|块|元|万|%|倍|次|分钟|小时)/g) || []);
    var emos = EMO.filter(function (e) { return t.indexOf(e) >= 0; });
    var qs = (t.match(/[？?]/g) || []).length;
    var emojis = (t.match(/[\u{1F300}-\u{1FAFF}\u{2600}-\u{27BF}]/gu) || []).length;
    var hasSave = /(收藏|存下|码住|先存|马住)/.test(t);
    var hasCTA = /(评论区|扣1|扣个|点赞|关注|三连|在看|转发|打卡|告诉我)/.test(t);
    var hasGold = lines.some(function (l) { return l.length >= 12 && l.length <= 45 && /(从来不是|真正的|其实是|才是|不需要|与其)/.test(l); });
    var listed = (t.match(/(^|\n)\s*(\d+[.、）)]|[1-9]️⃣|第[一二三四五六七八九十\d]+[步条点])/g) || []).length;

    var score = 0;
    score += hooks[0].n !== '平铺直叙' ? 25 : 5;
    score += Math.min(20, nums.length * 5);
    score += Math.min(15, emos.length * 4);
    score += hasSave ? 12 : 0;
    score += hasCTA ? 15 : 0;
    score += hasGold ? 8 : 0;
    score += listed >= 3 ? 10 : listed * 3;
    score = Math.min(99, score);

    var missing = [];
    if (hooks[0].n === '平铺直叙') missing.push('开头缺钩子：前两句没有让读者对号入座的具体场景');
    if (!nums.length) missing.push('全文没有具体数字：加「3个/7天/89元」这种可验证的量，可信度立刻上一档');
    if (!emos.length) missing.push('情绪浓度偏低：缺少能引发共鸣的情绪词');
    if (!hasSave) missing.push('没有引导收藏：中段插一句「先收藏再看」，收藏率通常能提升');
    if (!hasCTA) missing.push('结尾没有互动指令：改成二选一的低成本提问');
    if (!hasGold) missing.push('缺一句金句：读者转发时需要一句能替他表达的话');
    if (listed < 3) missing.push('结构不够清晰：拆成编号条目，完读率和收藏率都会好看');

    // 提炼公式
    var formula = [];
    formula.push('① 开头：' + hooks[0].n + (nums.length ? ' + 数字锚点' : ''));
    formula.push('② 承接：' + (hasSave ? '收藏引导（降低跳出）' : '（缺）建议补收藏引导'));
    formula.push('③ 主体：' + (listed >= 3 ? listed + ' 个编号条目，逐条给动作' : '段落式叙述，建议改成编号'));
    formula.push('④ 情绪：' + (emos.length ? '出现 ' + emos.slice(0, 5).join('/') + ' 等情绪词' : '（缺）情绪偏平'));
    formula.push('⑤ 结尾：' + (hasCTA ? '有明确互动指令' : '（缺）没有引导动作'));

    return {
      score: score, words: words, lines: lines.length,
      hook: hooks[0], hookAll: hooks,
      nums: nums, emos: emos, qs: qs, emojis: emojis,
      hasSave: hasSave, hasCTA: hasCTA, hasGold: hasGold, listed: listed,
      missing: missing, formula: formula,
      points: extractPoints(t)
    };
  }

  function extractPoints(t) {
    var out = [];
    t.split('\n').forEach(function (l) {
      l = l.trim();
      if (!l) return;
      if (/^(\d+[.、）)]|[1-9]️⃣|第[一二三四五六七八九十\d]+[步条点])/.test(l)) {
        out.push(l.replace(/^(\d+[.、）)]|[1-9]️⃣|第[一二三四五六七八九十\d]+[步条点][:：]?)/, '').trim());
      }
    });
    if (out.length < 2) {
      t.split(/[。！!\n]/).forEach(function (s) {
        s = s.trim();
        if (s.length > 10 && s.length < 60 && out.length < 6) out.push(s);
      });
    }
    return out.slice(0, 8);
  }

  /* 基于拆解结果改写 */
  function rewrite(txt, opt) {
    var a = analyze(txt);
    if (!a) return null;
    var pts = a.points.filter(Boolean);
    var n = Math.max(3, Math.min(8, pts.length || 5));
    var lines = pts.slice(0, n).map(function (p) { return p.slice(0, 26); }).join('\n');
    return generate({
      topic: opt.topic || (a.points[0] || '这个话题').slice(0, 14),
      platform: opt.platform || '小红书',
      type: opt.type || (a.listed >= 3 ? '清单式' : '分点式'),
      n: n,
      hook: opt.hook || (a.hook.n === '反差钩子' ? '反差' : '痛点'),
      audience: opt.audience,
      trend: opt.trend,
      points: lines
    });
  }

  /* ---------------- 素材库改写：短视频脚本（3秒钩子→15秒价值→10秒案例→CTA收口） ---------------- */
  function buildCase(topic, v) {
    var who = R(['粉丝', '学员', '一个做自媒体的朋友', '我自己', '评论区一个姐妹', '刚毕业的同事']);
    var situation = R([
      '之前' + topic + '做了三个月一点水花，差点放弃',
      '一直卡在' + topic + '这步，收藏一堆就是不动手',
      topic + '试了七八个方法全扑街，心态崩了',
      '刚开始做' + topic + '，0 基础 0 粉丝，啥都不懂'
    ]);
    var res = R([
      '两周后那条跑出 1.2w 播放，涨了 800 粉',
      '第三天就接到第一单，变现 600 块',
      '一个月攒了 30 条素材，账号从 200 粉到 5000 粉',
      '照做一周，完播率从 18% 提到 46%'
    ]);
    var tpls = [
      '说个真事：我那个' + who + '，' + situation + '。后来就死磕「' + v.t + '」这一条——' + v.d.replace(/。.*/, '') + '。结果' + res + '。',
      '评论区有个' + who + '跟我说，' + situation + '。我让她只改一个点：' + v.t + '。一周后回来报喜：' + res + '。',
      '前两天帮一个' + who + '跑通，' + situation + '。核心就一句：' + v.t + '。' + res + '。'
    ];
    return R(tpls) + '（这是示例案例，换成你自己的真实故事更有说服力）';
  }

  function scriptRewrite(txt, opt) {
    var a = analyze(txt);
    if (!a) return null;
    var topic = (opt.topic || (a.points[0] || '这个话题')).slice(0, 16);
    var platform = opt.platform || '抖音';
    var aud = opt.audience || R(AUD);
    var libKey = matchLib(topic), lib = LIB[libKey];

    // 价值要点：优先用素材提取出的条目，不足用赛道库补齐
    var pts = a.points.filter(Boolean);
    var n = Math.max(3, Math.min(5, pts.length || 3));
    var vals = [];
    for (var i = 0; i < n; i++) {
      var rawp = pts[i] || (lib.step[i % lib.step.length] || lib.step[0]).t;
      var item = lib.step[i % lib.step.length] || lib.step[0];
      vals.push({ t: String(rawp).slice(0, 24), d: item.d, tip: item.tip });
    }

    // 3秒钩子
    var hookStyle = opt.hook || (a.hook.n === '反差钩子' ? '反差' : (a.hook.n === '数字钩子' ? '数字' : '痛点'));
    var vars = { topic: topic, n: n, k: 1 + Math.floor(Math.random() * n), aud: aud, time: R(TIME), bad: R(BAD), wrong: '硬啃' + topic };
    var hook = fill(R(HOOK[hookStyle] || HOOK['痛点']), vars);

    // 10秒案例
    var caseStory = buildCase(topic, vals[0]);

    // CTA收口
    var ctaArr = CTA[platform] || CTA['抖音'];
    var cta = ctaArr[Math.floor(Math.random() * ctaArr.length)];

    // 视频主题标题
    var tps = TITLE[platform] || TITLE['抖音'];
    var title = fill(R(tps), vars);

    var script = [
      { t: '0-3s', label: '3秒钩子', role: 'hook',
        lines: [hook],
        visual: '第一帧大字卡或正脸出镜，静音也能看懂；表情要给反应（惊讶/无奈/笃定）' },
      { t: '3-18s', label: '15秒价值', role: 'value',
        lines: vals.map(function (v, i) {
          return '第' + (i + 1) + '点 · ' + v.t + '：' + v.d + ' 👉 提醒：' + v.tip;
        }),
        visual: '口播 + 字幕条，关键词打在屏上；每点切一个画面或录屏演示' },
      { t: '18-28s', label: '10秒案例', role: 'case',
        lines: [caseStory],
        visual: '真实场景实拍 / 手机录屏 / 前后对比截图，证明刚才说的真能落地' },
      { t: '28-33s+', label: 'CTA收口', role: 'cta',
        lines: [cta],
        visual: '看镜头 + 手势引导；金句大字定格 2 秒，别用「谢谢观看」收尾' }
    ];

    var body = ['【视频主题】' + title, ''];
    script.forEach(function (s) {
      body.push('▶ [' + s.t + ' · ' + s.label + ']');
      s.lines.forEach(function (l) { body.push(l); });
      body.push('🎬 画面：' + s.visual);
      body.push('');
    });
    var text = body.join('\n').trim().replace(/\n{3,}/g, '\n\n');

    var baseTags = ['#' + topic.replace(/\s/g, ''), '#短视频脚本', '#' + lib.name.split(' / ')[0], '#' + platform + '运营', '#' + aud];
    var tags = baseTags.filter(function (v, i, arr) { return arr.indexOf(v) === i; }).slice(0, 8).join(' ');

    return { title: title, platform: platform, body: text, tags: tags, script: script, meta: { lib: lib.name, type: '脚本', platform: platform, words: text.length } };
  }

  /* ---------------- 导出给外部 AI 用的提示词 ---------------- */
  function toPrompt(opt) {
    var t = opt.topic || '', p = opt.platform || '小红书', ty = opt.type || '步骤式';
    var n = opt.n || 5, aud = opt.audience || '新手', hk = opt.hook || '痛点';
    var trend = opt.trend;
    return [
      '你是一位' + p + '爆款内容操盘手，擅长写让人一眼停下、看完就想收藏的口语化干货。',
      '',
      '【本次任务】',
      '话题：' + t,
      '平台：' + p,
      '目标读者：' + aud,
      '干货形式：' + ty + '（' + (ty === '步骤式' ? '按先后顺序拆步骤' : ty === '清单式' ? '并列清单，可跳读' : '分点讲观点，每点一个立场') + '）',
      '条数：正好 ' + n + ' 条，标题里写几条正文就必须是几条，不能对不上。',
      '开头钩子风格：' + hk + '型',
      trend ? '今日同赛道真实爆款参考：「' + trend.title + '」（' + (trend.why || '') + '），请把这条的情绪切口/钩子方式化用进来，但不要照抄它的内容。' : '',
      '',
      '【必须遵守的结构】',
      '1. 开头 1-3 句戳痛点，让目标读者立刻对号入座。禁止问好、禁止自我介绍、禁止铺垫背景。',
      '2. 紧接一句自然的收藏引导，不要生硬喊「求收藏」。',
      '3. 正文 ' + n + ' 条干货，每条包含：一个明确的小标题 + 具体怎么做（有动作、有数字、有场景）+ 一句「小提醒」（说明容易踩的坑）。',
      '4. 一句金句，12-30 字，能被单独截图转发。',
      '5. 结尾行动号召：给一个当天就能做的动作 + 一个低成本互动问题（二选一式，不要开放式）。',
      '6. 最后给 5-8 个话题标签。',
      '',
      '【语言要求】',
      '- 口语化，像跟朋友聊天。多用短句，允许出现「真的」「说实话」「反正我是」这类口头语。',
      '- 禁止 AI 腔：不要排比句堆砌，不要「不仅…而且…」，不要没有主语的宏大总结，不要「在这个快节奏的时代」。',
      '- 每条至少出现一个具体数字或具体场景，不要停留在「要重视」「很重要」。',
      '- 融入真实感：可以写自己踩过的坑、花过的冤枉钱、具体到几点几分的场景。',
      '- 必须原创，不要照搬任何参考内容的句子。',
      '',
      '【输出格式】',
      '先给 4 个备选标题（都要含数字 ' + n + '，20 字内，可带 1-2 个 emoji），再给完整正文，最后给标签。'
    ].filter(Boolean).join('\n');
  }

  w.E = {
    generate: generate, analyze: analyze, rewrite: rewrite, scriptRewrite: scriptRewrite, toPrompt: toPrompt,
    LIB: LIB, matchLib: matchLib
  };
})(window);
